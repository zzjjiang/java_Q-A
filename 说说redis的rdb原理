说说redis的rdb原理。假设服务器的内存8g，redis父进程占用了6g
这是一道挖坑题。内存占用只会比6g多一点，不会用12g。
fork+  copy on write。

ps: 同一块
rdb 是redis的持久化机制之一，一般开辟一个新进程会有自己的内存空间，
这样持久化的子进程可能内存不足，有一种fork开启子进程的方法，
父子进程指向的是同样的内存地址，当父进程有写操作时，父进程会指向新的内存地址，这样就减少了内存的问题，同时这里采用了copyonwrite
